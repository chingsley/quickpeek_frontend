/**
 * Shared Searchbar chrome — logo leading icon size, search icon size, and spacing.
 * Used by `components/Searchbar.tsx` on every screen (Home, Chats, …).
 */
export const SEARCHBAR_LEADING_LOGO_SIZE = 30;

/** Matches Ionicons `search-outline` when `leading="search"`. */
export const SEARCHBAR_LEADING_ICON_SIZE = 20;

/** Space between the leading adornment and the text input. */
export const SEARCHBAR_LEADING_GAP = 10;
